// Requires gradle 7.2
// gradle clean build
// gradle publishToMavenLocal

allprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

ext {
    releaseFileName = "${projectName}-${version}.zip"
}
